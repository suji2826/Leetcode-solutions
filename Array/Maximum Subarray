class Solution {
    public int maxSubArray(int[] nums) {
        int max = nums[0];
        int curr = 0;
        for(int a:nums){
            if(curr<0){
                curr = 0;
            }
            curr = curr + a;
            max = Math.max(curr,max);
        }
        return max;
    }
}
